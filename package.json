{
  "name": "prediction-league",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:wallet",
    "build:wallet": "esbuild web3/wallet-entry.mjs --bundle --format=iife --global-name=PLWallet --outfile=wallet.bundle.js --platform=browser --legal-comments=none",
    "prestart": "npm run build:wallet",
    "predev": "npm run build:wallet",
    "start": "node server.mjs",
    "dev": "node server.mjs"
  },
  "dependencies": {
    "@wagmi/core": "^3.4.1",
    "dotenv": "^17.3.1",
    "viem": "^2.47.6"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
